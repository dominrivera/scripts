# Clean host key script

* This script clean deprecated host keys stored in your `~/.ssh/known_hosts` file.

* To run the script you just have to execute `sh clean_hostkey <hostname>` and your key will disappear.
